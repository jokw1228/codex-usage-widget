{
  "name": "codex-usage-widget",
  "version": "0.1.3",
  "private": true,
  "description": "Always-on-top Codex usage widget for Windows.",
  "author": "Kangwoo Cho",
  "license": "MIT",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js && node --check scripts/create-icon.js",
    "icon:build": "electron scripts/create-icon.js",
    "package:win": "node scripts/clean-release.js before && npm run icon:build && electron-builder --win portable --x64 && node scripts/clean-release.js after"
  },
  "build": {
    "appId": "dev.local.codexUsageWidget",
    "productName": "Codex Usage Widget",
    "asar": true,
    "artifactName": "codex-usage-widget-${version}-portable-${arch}.${ext}",
    "directories": {
      "output": "release"
    },
    "files": [
      "src/**/*",
      "assets/app-icon.ico",
      "package.json"
    ],
    "icon": "assets/app-icon.ico",
    "win": {
      "target": [
        "portable"
      ]
    },
    "publish": null
  },
  "devDependencies": {
    "electron": "^44.2.0",
    "electron-builder": "^26.0.12"
  }
}
